{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"openalex-snapshot Documentation","text":"<p>This folder contains internet-facing documentation for <code>openalex-snapshot</code>.</p> <p>Argument precedence (highest wins): CLI arguments &gt; config subcommand section &gt; config <code>defaults</code> section &gt; built-in defaults.</p> <p>Recommended page order:</p> <ol> <li><code>quickstart.md</code></li> <li><code>commands/check.md</code></li> <li><code>commands/config.md</code></li> <li><code>commands/all.md</code></li> <li><code>commands/download.md</code></li> <li><code>commands/verify_download.md</code></li> <li><code>commands/convert.md</code></li> <li><code>commands/verify.md</code></li> <li><code>commands/repair.md</code></li> <li><code>commands/index.md</code></li> <li><code>commands/verify_index.md</code></li> <li><code>commands/extract.md</code></li> <li><code>commands/schema.md</code></li> <li><code>commands/verify_schema.md</code></li> <li><code>commands/report.md</code></li> <li><code>commands/prune-reports.md</code></li> <li><code>commands/progress.md</code></li> <li><code>commands/skills.md</code></li> <li><code>operations/low-memory.md</code></li> <li><code>operations/cache-precompute.md</code></li> <li><code>operations/recovery.md</code></li> <li><code>troubleshooting.md</code></li> </ol>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#build","title":"Build","text":"<pre><code>cd openalex-snapshot\ncargo build --release\n./target/release/openalex-snapshot --help\n</code></pre>"},{"location":"quickstart/#preflight","title":"Preflight","text":"<pre><code>./target/release/openalex-snapshot check \\\n  --root-dir /Volumes/openalex \\\n  --dataset all\n</code></pre>"},{"location":"quickstart/#convert-snapshot-to-parquet","title":"Convert snapshot to parquet","text":"<pre><code># default profile (safe) \u2014 works on any host\n./target/release/openalex-snapshot convert \\\n  --root-dir /Volumes/openalex \\\n  --dataset works\n\n# faster on 32+ GB hosts (stratifies the file list by size)\n./target/release/openalex-snapshot convert \\\n  --root-dir /Volumes/openalex \\\n  --dataset works \\\n  --profile stratified-36\n</code></pre>"},{"location":"quickstart/#verify-output","title":"Verify output","text":"<pre><code>./target/release/openalex-snapshot verify_convert \\\n  --root-dir /Volumes/openalex \\\n  --dataset works \\\n  --scope dataset \\\n  --metadata-level both\n</code></pre>"},{"location":"quickstart/#build-and-verify-index","title":"Build and verify index","text":"<pre><code>./target/release/openalex-snapshot index \\\n  --root-dir /Volumes/openalex \\\n  --dataset works\n\n./target/release/openalex-snapshot verify_index \\\n  --root-dir /Volumes/openalex \\\n  --dataset works\n</code></pre>"},{"location":"quickstart/#extract-by-ids","title":"Extract by IDs","text":"<pre><code>./target/release/openalex-snapshot extract \\\n  --root-dir /Volumes/openalex \\\n  --ids /Volumes/openalex/ids.csv \\\n  --output /Volumes/openalex/extract.parquet\n</code></pre>"},{"location":"quickstart/#download-and-verify-snapshot","title":"Download and verify snapshot","text":"<pre><code>./target/release/openalex-snapshot download --root-dir /Volumes/openalex\n./target/release/openalex-snapshot verify_download --root-dir /Volumes/openalex\n</code></pre>"},{"location":"quickstart/#repair-failed-files-from-verify-report","title":"Repair failed files from verify report","text":"<pre><code>./target/release/openalex-snapshot repair_convert \\\n  --root-dir /Volumes/openalex \\\n  --from-verify-report /Volumes/openalex/openalex-snapshot_metadata/reports/verify_convert-123456.json\n</code></pre>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#out-of-memory-in-convert","title":"Out of memory in convert","text":"<p><code>safe</code> is the default profile and should handle the largest files (\u22481 GB compressed) on any host via DuckDB spill-to-disk. If you still observe OOM:</p> <ul> <li>Confirm you're on the default profile: drop <code>--profile &lt;name&gt;</code> to inherit <code>safe</code>.</li> <li>Lower the memory cap so DuckDB spills earlier: <code>--max-memory-mb 2048</code>.</li> <li>Pre-split very large gz files: <code>--split-size 256mb</code> (produces numbered parquets per chunk).</li> <li>Isolate the suspect file with <code>--input-file &lt;rel-path&gt;</code> and retry.</li> </ul> <p>See <code>docs/operations/low-memory.md</code> for the full runbook.</p>"},{"location":"troubleshooting/#verify-appears-to-slow-down-over-time","title":"Verify appears to slow down over time","text":"<p>Expected when remaining files are larger and <code>id-hash</code> is enabled. Progress is item-based, not byte-based.</p>"},{"location":"troubleshooting/#mixed-oldnew-metadata-folders","title":"Mixed old/new metadata folders","text":"<p>Current canonical location is:</p> <ul> <li><code>openalex-snapshot_metadata/</code></li> </ul> <p>Expected structure:</p> <ul> <li><code>openalex-snapshot_metadata/reports/</code> \u2014 latest report per command</li> <li><code>openalex-snapshot_metadata/archived/&lt;timestamp&gt;/</code> \u2014 previous runs</li> <li><code>openalex-snapshot_metadata/download/download.log</code></li> <li><code>openalex-snapshot_metadata/&lt;dataset&gt;/schemata/</code></li> <li><code>openalex-snapshot_metadata/&lt;dataset&gt;/convert/</code></li> <li><code>openalex-snapshot_metadata/&lt;dataset&gt;/conversion-verify/</code></li> <li><code>openalex-snapshot_metadata/&lt;dataset&gt;/index/</code></li> <li><code>openalex-snapshot_metadata/&lt;dataset&gt;/index-verify/</code></li> </ul>"},{"location":"troubleshooting/#schema-errors-around-nested-types","title":"Schema errors around nested types","text":"<p>Ensure canonical schema cache exists:</p> <ul> <li><code>.&lt;dataset&gt;_metadata/schemata/unified_schema.csv</code></li> </ul> <p>If stale, re-run with schema refresh options.</p>"},{"location":"commands/","title":"Command: index","text":"<p>Build a parquet lookup index equivalent to R <code>build_corpus_index()</code>.</p> <p>Default dataset is <code>all</code> (build indexes for all datasets). Use <code>--dataset &lt;name&gt;</code> to limit to one dataset. When running with <code>--dataset all</code>, existing per-dataset index files are skipped and missing ones are built. If <code>--index-file</code> is supplied, it is ignored in <code>all</code> mode.</p>"},{"location":"commands/#usage","title":"Usage","text":"<pre><code>openalex-snapshot index \\\n  --root-dir /data \\\n  --dataset works\n</code></pre>"},{"location":"commands/#output-columns","title":"Output columns","text":"<ul> <li><code>id</code></li> <li><code>id_block</code></li> <li><code>parquet_file</code></li> <li><code>file_row_number</code></li> </ul>"},{"location":"commands/#tuning","title":"Tuning","text":"<p><code>index</code> reads parquet files (already-converted output of <code>convert</code>), so its memory needs are modest compared to <code>convert</code>. The default tuning is fine for any host with \u22654 GB RAM. Use <code>--max-memory-mb &lt;N&gt;</code> and <code>--workers &lt;N&gt;</code> only if you need to constrain resources.</p> <p>Note: the legacy <code>--profile safe|balanced|fast</code> flag is accepted on this command for backwards compatibility but doesn't drive stratified parallelism. The stratified profile system (<code>safe</code>, <code>stratified-36</code>, custom <code>stratified-N</code> from <code>openalex-snapshot.performance.yaml</code>) currently applies only to <code>convert</code> and <code>repair_convert</code>. See <code>convert.md</code>.</p>"},{"location":"commands/all/","title":"Command: all","text":"<p>Run the full pipeline from config with a bounded <code>verify_convert</code>/<code>repair_convert</code> retry loop.</p>"},{"location":"commands/all/#usage","title":"Usage","text":"<pre><code>openalex-snapshot all --config ./openalex-snapshot.yaml --retry 2\n</code></pre>"},{"location":"commands/all/#flow","title":"Flow","text":"<ol> <li><code>check</code> (if enabled)</li> <li><code>download</code> (if enabled)</li> <li><code>verify_download</code> (if enabled)</li> <li><code>convert</code></li> <li><code>verify_convert</code></li> <li><code>repair_convert</code> (only when verify fails, up to <code>--retry</code>)</li> <li><code>index</code></li> <li><code>verify_index</code></li> </ol>"},{"location":"commands/check/","title":"check","text":"<p>Run environment and capacity preflight checks before heavy operations.</p>"},{"location":"commands/check/#usage","title":"Usage","text":"<pre><code>openalex-snapshot check --root-dir . --dataset all\n</code></pre>"},{"location":"commands/check/#what-it-checks","title":"What it checks","text":"<ul> <li>dependency binaries (<code>aws</code> \u2014 <code>duckdb</code> is bundled and reported as ok)</li> <li>path writability for root/snapshot/parquet/metadata</li> <li>download disk requirement (remote manifest + safety margin)</li> <li>convert disk requirement (precise source inventory estimate)</li> <li>memory/tuning risk based on profile/workers/memory settings</li> </ul>"},{"location":"commands/check/#options","title":"Options","text":"<ul> <li><code>--strict</code> fail on warnings as well as failures</li> <li><code>--json</code> machine-readable output</li> <li><code>--precise</code> enable precise source inventory estimate</li> <li>shared runtime args: <code>--dataset</code>, <code>--workers</code>, <code>--max-memory-mb</code></li> </ul>"},{"location":"commands/config/","title":"Command: config","text":"<p>Create or verify <code>openalex-snapshot</code> YAML config files.</p>"},{"location":"commands/config/#usage","title":"Usage","text":"<pre><code>openalex-snapshot config --create complete\nopenalex-snapshot config --create safe --config ./openalex-snapshot-safe.yaml\nopenalex-snapshot config --verify --config ./openalex-snapshot.yaml\n</code></pre>"},{"location":"commands/config/#notes","title":"Notes","text":"<ul> <li><code>--create</code> templates: <code>complete</code> (default), <code>safe</code>, <code>fast</code></li> <li>precedence: CLI &gt; command section &gt; defaults section &gt; built-in defaults</li> <li><code>--verify</code> fails on unknown keys or wrong types</li> </ul>"},{"location":"commands/convert/","title":"Command: convert","text":"<p>Convert OpenAlex snapshot <code>.json.gz</code> files into parquet while preserving relative structure.</p>"},{"location":"commands/convert/#usage","title":"Usage","text":"<pre><code># default profile (safe) \u2014 single-worker, max-memory; works on any host\nopenalex-snapshot convert \\\n  --root-dir /data \\\n  --dataset works\n\n# stratified profile (recommended for 32+ GB hosts) \u2014 partitions files by gz size\n# and parallelises each size bucket with its own worker count / memory budget\nopenalex-snapshot convert \\\n  --root-dir /data \\\n  --dataset works \\\n  --profile stratified-36\n</code></pre>"},{"location":"commands/convert/#key-behavior","title":"Key behavior","text":"<ul> <li>1 input <code>.gz</code> maps to 1 output <code>.parquet</code> (unless <code>--split-size</code> is set; see below)</li> <li>Resume-safe output skipping</li> <li>Works enrichment \u2014 for the <code>works</code> dataset, two derived columns are written   alongside the source columns: <code>abstract</code> (plain text, reconstructed from   <code>abstract_inverted_index</code>) and <code>citation</code> (<code>\"Author (year)\"</code> / <code>\"A &amp; B (year)\"</code> /   <code>\"A et al. (year)\"</code>).  <code>abstract_inverted_index</code> is kept as-is in the output.</li> <li>Auto-repair from the latest <code>verify_convert</code> report \u2014 see \"Auto-repair\" below.</li> <li>Verification is separate via <code>verify_convert</code></li> <li>Supports selected-file conversion via repeated <code>--input-file</code></li> <li>Per-stratum execution under stratified profiles \u2014 see \"Profile / tuning\" below</li> </ul>"},{"location":"commands/convert/#auto-repair-from-verify-report","title":"Auto-repair from verify report","text":"<p>At startup, <code>convert</code> reads the most-recent <code>verify_convert</code> report under <code>&lt;root&gt;/openalex-snapshot_metadata/reports/</code>.  For any parquet that report flagged (phase <code>verify_metrics</code> or <code>convert_file</code>), the existing parquet is deleted so the normal skip-if-exists filter re-includes that file in the convert pass.</p> <p>Net effect: running <code>convert</code> a second time fixes whatever <code>verify_convert</code> flagged. There is no separate <code>repair_convert</code> subcommand \u2014 convert is its own repair.</p> <p>Behaviour:</p> <ul> <li>Default: enabled.  No flag needed.</li> <li>Opt-out per-run: <code>--auto-repair=false</code>.</li> <li>Opt-out in config: <code>convert.auto_repair: false</code> in <code>openalex-snapshot.yaml</code>.</li> <li>Ignored when <code>--input-file</code> is given: if you explicitly name files, only those   are processed and the verify report is not consulted.  Most predictable for ad-hoc   work.</li> <li>No-op when there's no verify report (or when the report has no <code>verify_metrics</code>   / <code>convert_file</code> failures for the current datasets).</li> </ul> <p>In a pipeline (<code>all</code>), the orchestrator loops <code>convert \u2192 verify_convert</code> up to <code>--retry N</code> times.  Each retry's convert call auto-repairs whatever the prior verify flagged.</p>"},{"location":"commands/convert/#profile-tuning","title":"Profile / tuning","text":"<p><code>--profile</code> selects a built-in or user-defined performance profile.  Built-ins:</p> Profile Kind Behaviour <code>safe</code> (default) Single-pass 1 worker (clampable to 2), generous per-worker memory (45 % of usable RAM, clamped 8 \u2013 24 GiB on single-worker mode).  Conservative and works on any host. <code>stratified-36</code> Stratified Empirically tuned for ~36 GB RAM hosts.  Partitions the file list by gz size and runs one rayon parallel pass per non-empty stratum, largest-files-first. <p><code>stratified-36</code>'s strata (workers \u00d7 per-worker memory):</p> gz size Workers Per-worker mem &lt;400 MB 4 4 800 MB 400\u2013600 MB 3 6 400 MB 600\u2013800 MB 2 9 600 MB 800+ MB 1 13 000 MB <p>For hosts with different RAM than 36 GB, write a custom <code>performance.yaml</code> (see below) or use <code>safe</code>.</p>"},{"location":"commands/convert/#custom-profiles-via-performanceyaml","title":"Custom profiles via <code>performance.yaml</code>","text":"<p>A sibling YAML file <code>openalex-snapshot.performance.yaml</code> (in the working directory, or via the <code>--performance-config &lt;path&gt;</code> global flag) defines additional named profiles.  User-defined names with the same name as a built-in override it.</p> <p>Fastest path \u2014 scaffold a profile auto-derived from your host's RAM:</p> <pre><code>openalex-snapshot config --create-profiles\n# writes ./openalex-snapshot.performance.yaml with a `stratified-&lt;RAM_GB&gt;` profile,\n# plus commented examples for half- and double-RAM tiers.\n</code></pre> <p>To preview what would be written without creating the file:</p> <pre><code>openalex-snapshot config --create-profiles --stdout\n</code></pre> <p>To inspect what profiles are visible (built-ins + anything loaded from your <code>performance.yaml</code>):</p> <pre><code>openalex-snapshot config --list-profiles\n</code></pre> <p>The generated file's schema:</p> <pre><code># openalex-snapshot.performance.yaml\nprofiles:\n  stratified-16:\n    description: \"Tuned for ~16 GB RAM\"\n    min_ram_gb: 12\n    kind: stratified\n    strata:\n      - max_file_mb: 400\n        workers: 2\n        per_worker_mb: 3000\n      - max_file_mb: 800\n        workers: 1\n        per_worker_mb: 5500\n      - workers: 1            # max_file_mb omitted = catch-all (no upper bound)\n        per_worker_mb: 6500\n</code></pre> <p>Then run <code>openalex-snapshot convert --profile stratified-16 \u2026</code>.</p>"},{"location":"commands/convert/#overrides","title":"Overrides","text":"<ul> <li><code>--max-memory-mb N</code> overrides every stratum's per-worker memory cap.</li> <li><code>--workers N</code> collapses a stratified profile into a single flat parallel pass with N workers and the largest stratum's memory.  Use only when you know all your files fit one bucket.</li> </ul>"},{"location":"commands/convert/#large-file-handling","title":"Large-file handling","text":"<p>By default (<code>--split-size 0</code>) large files are processed directly by in-process DuckDB, which streams and spills to disk as needed within the per-stratum memory budget.  The spill directory is at <code>&lt;root&gt;/openalex-snapshot_metadata/duckdb_tmp/</code> (created automatically).</p> <p>Set <code>--split-size &lt;SIZE&gt;</code> (e.g. <code>256mb</code>, <code>512mb</code>) to pre-split gz files larger than that threshold into chunks before conversion.  Each chunk produces a numbered parquet file (e.g. <code>part_0000_001.parquet</code>, <code>part_0000_002.parquet</code>).  Use this only if you observe OOM despite a generous profile, or when running on a machine with very limited RAM.</p>"},{"location":"commands/convert/#reading-the-run-log","title":"Reading the run log","text":"<p>Each dataset prints two key lines:</p> <pre><code>[convert] dataset=works profile=stratified-36 strata=4 flat=false\n[convert] dataset=works stratum 1/4: files=54 workers=1 per_worker_mb=13000\n[convert] dataset=works stratum 2/4: files=35 workers=2 per_worker_mb=9600\n...\n</code></pre> <ul> <li><code>flat=false</code> \u21d2 stratified mode is active (file list partitioned across strata).</li> <li><code>flat=true</code> \u21d2 a single parallel pass (Safe profile, or stratified collapsed by <code>--workers</code>).</li> <li>Strata are emitted largest-files-first so risky files surface failures early.</li> </ul>"},{"location":"commands/download/","title":"Command: download","text":"<p>Sync OpenAlex snapshot from S3 via AWS CLI.</p> <p>Validation is a separate step via <code>verify_download</code>.</p>"},{"location":"commands/download/#usage","title":"Usage","text":"<pre><code>openalex-snapshot download --root-dir /data\n</code></pre> <p>Default sync intent:</p> <pre><code>aws s3 sync --delete s3://openalex /data/openalex-snapshot --no-sign-request\n</code></pre>"},{"location":"commands/extract/","title":"Command: extract","text":"<p>Extract records by OpenAlex IDs from CSV using per-dataset parquet indexes.</p> <p>The command auto-routes each ID to a dataset using:</p> <ul> <li>entity prefixes (<code>W</code>, <code>A</code>, <code>S</code>, <code>I</code>, <code>T</code>, <code>K</code>, <code>P</code>, <code>F</code>, <code>G</code>, <code>C</code>)</li> <li>taxonomy namespaces (<code>countries/</code>, <code>continents/</code>, <code>languages/</code>, <code>domains/</code>, <code>fields/</code>, <code>subfields/</code>, <code>sdgs/</code>, <code>work-types/</code>, <code>source-types/</code>, <code>licenses/</code>, <code>institution-types/</code>)</li> </ul> <p>It writes one output file per resolved dataset as:</p> <ul> <li><code>&lt;output_base&gt;_&lt;dataset&gt;.parquet</code></li> </ul> <p>Unknown/unmapped IDs are skipped and reported.</p>"},{"location":"commands/extract/#example","title":"Example","text":"<pre><code>openalex-snapshot extract \\\n  --root-dir /Volumes/openalex \\\n  --ids /Volumes/openalex/ids.csv \\\n  --output /Volumes/openalex/extract.parquet\n</code></pre>"},{"location":"commands/extract/#notes","title":"Notes","text":"<ul> <li>Requires per-dataset index files (for example <code>parquet/works_id_idx.parquet</code>).</li> <li>Build indexes first with <code>openalex-snapshot index</code>.</li> <li>Use <code>--dataset &lt;name&gt;</code> to restrict extraction to one dataset.</li> </ul>"},{"location":"commands/progress/","title":"Command: progress","text":"<p>Monitor active/recent runs from report/log files, including from another terminal session.</p>"},{"location":"commands/progress/#usage","title":"Usage","text":"<pre><code>openalex-snapshot progress --root-dir /data\nopenalex-snapshot progress --root-dir /data --once\nopenalex-snapshot progress --root-dir /data --command convert --dataset works\n</code></pre>"},{"location":"commands/progress/#notes","title":"Notes","text":"<ul> <li>default mode is live watch</li> <li><code>--once</code> prints one snapshot and exits</li> <li><code>--json</code> emits machine-readable output</li> </ul>"},{"location":"commands/prune-reports/","title":"Command: prune-reports","text":"<p>Remove older report files and keep the latest set.</p>"},{"location":"commands/prune-reports/#usage","title":"Usage","text":"<pre><code>openalex-snapshot prune-reports --root-dir /data\n</code></pre>"},{"location":"commands/prune-reports/#notes","title":"Notes","text":"<ul> <li>keeps recent reports and deletes older ones according to command policy</li> <li>use before long-running batches to reduce metadata noise</li> </ul>"},{"location":"commands/report/","title":"Command: report","text":"<p>Show run reports written under <code>openalex-snapshot_metadata</code>.</p>"},{"location":"commands/report/#usage","title":"Usage","text":"<pre><code># show latest reports with per-dataset breakdown (default)\nopenalex-snapshot --config ./openalex-snapshot.yaml report --latest\n\n# show only aggregate totals (suppress per-dataset rows)\nopenalex-snapshot --config ./openalex-snapshot.yaml report --latest --summary\n\n# show full JSON details\nopenalex-snapshot --config ./openalex-snapshot.yaml report --latest --full\n\n# list available archived run timestamps\nopenalex-snapshot report --root-dir /data --list\n\n# show a specific archived run\nopenalex-snapshot report --root-dir /data --archived 1715000000 --latest\n\n# filter by command\nopenalex-snapshot report --root-dir /data --command verify_convert --latest\n</code></pre>"},{"location":"commands/report/#default-output","title":"Default output","text":"<p>Each report is shown as a header line followed by a per-dataset table:</p> <pre><code>=== convert [2026-05-09 00:28:53  40m18s  ok]  convert-1778260662.json\n  dataset                scanned       ok   failed  skipped\n  ------------------------------------------------------\n  authors                    546      546        0        0\n  works                     2127     2127        0        0  !\n</code></pre> <p>Datasets with failures are marked with <code>!</code>.</p>"},{"location":"commands/report/#notes","title":"Notes","text":"<ul> <li><code>--config</code> is a global flag and must precede the subcommand:   <code>openalex-snapshot --config ./openalex-snapshot.yaml report</code></li> <li><code>--summary</code> prints only aggregate totals (old behavior); suppresses per-dataset rows</li> <li><code>--list</code> shows timestamps available under <code>openalex-snapshot_metadata/archived/</code></li> <li><code>--archived &lt;timestamp&gt;</code> reads <code>openalex-snapshot_metadata/archived/&lt;timestamp&gt;/reports/</code></li> <li><code>--command &lt;name&gt;</code> filters by command (e.g. <code>verify_convert</code>, <code>repair_convert</code>)</li> <li><code>--full</code> prints full JSON details</li> </ul>"},{"location":"commands/schema/","title":"Command: schema","text":"<p>Inspect schema from source/cache/parquet and optionally diff schema sources.</p>"},{"location":"commands/schema/#usage","title":"Usage","text":"<pre><code>openalex-snapshot schema \\\n  --root-dir /data \\\n  --dataset works \\\n  --from auto \\\n  --format arrow-r\n</code></pre>"},{"location":"commands/schema/#notes","title":"Notes","text":"<ul> <li>canonical cache: <code>openalex-snapshot_metadata/&lt;dataset&gt;/schemata/unified_schema.csv</code></li> <li><code>arrow-r</code> format is stable JSON for R Arrow comparisons</li> </ul>"},{"location":"commands/skills/","title":"skills","text":"<p>Create a project-local <code>skills/</code> starter pack for AI coding agents.</p>"},{"location":"commands/skills/#usage","title":"Usage","text":"<pre><code>openalex-snapshot skills --root-dir .\n</code></pre>"},{"location":"commands/skills/#options","title":"Options","text":"<ul> <li><code>--root-dir &lt;path&gt;</code> root containing snapshot/parquet/metadata layout</li> <li><code>--overwrite</code> rewrite generated template files</li> <li><code>--stdout</code> print template manifest/content</li> <li><code>--explain</code> print planned actions only</li> </ul>"},{"location":"commands/skills/#behavior","title":"Behavior","text":"<ul> <li>Default mode is non-destructive: missing files are created, existing files are kept.</li> <li>Generated content focuses on command operation, pipeline runbook, recovery, and docs hygiene.</li> </ul>"},{"location":"commands/verify/","title":"Command: verify_convert","text":"<p>Verify snapshot/parquet parity for structure and file-level metrics.</p>"},{"location":"commands/verify/#usage","title":"Usage","text":"<pre><code>openalex-snapshot verify_convert \\\n  --root-dir /data \\\n  --dataset works \\\n  --scope dataset \\\n  --metadata-level both\n</code></pre>"},{"location":"commands/verify/#scope","title":"Scope","text":"<ul> <li><code>file</code>: sampled file-pair checks</li> <li><code>dataset</code>: all files in one dataset</li> <li><code>snapshot</code>: all selected datasets</li> </ul>"},{"location":"commands/verify/#metadata-levels","title":"Metadata levels","text":"<ul> <li><code>row-count</code></li> <li><code>id-hash</code></li> <li><code>both</code></li> </ul>"},{"location":"commands/verify_download/","title":"Command: verify_download","text":"<p>Strictly validate downloaded snapshot integrity.</p>"},{"location":"commands/verify_download/#usage","title":"Usage","text":"<pre><code>openalex-snapshot verify_download --root-dir /data\n</code></pre>"},{"location":"commands/verify_download/#checks","title":"Checks","text":"<ul> <li>remote manifest parity</li> <li>missing files</li> <li>size mismatches</li> <li>optional extra local files</li> <li>gzip integrity for <code>.json.gz</code></li> </ul>"},{"location":"commands/verify_index/","title":"Command: verify_index","text":"<p>Verify index integrity and coverage for one dataset or all datasets.</p>"},{"location":"commands/verify_index/#usage","title":"Usage","text":"<pre><code>openalex-snapshot verify_index \\\n  --root-dir /data \\\n  --dataset all\n</code></pre>"},{"location":"commands/verify_index/#checks","title":"Checks","text":"<ul> <li>required index columns are present</li> <li>referenced parquet files exist</li> <li>row-number references are valid</li> </ul>"},{"location":"commands/verify_schema/","title":"Command: verify_schema","text":"<p>Verify schema parity across schema sources.</p>"},{"location":"commands/verify_schema/#usage","title":"Usage","text":"<pre><code>openalex-snapshot verify_schema \\\n  --root-dir /data \\\n  --dataset works \\\n  --left source \\\n  --right parquet\n</code></pre>"},{"location":"commands/verify_schema/#notes","title":"Notes","text":"<ul> <li>exits non-zero when schema differences are found</li> <li>default comparison is source vs parquet</li> </ul>"},{"location":"operations/cache-precompute/","title":"Runbook: Precompute Verify Caches","text":"<pre><code>openalex-snapshot verify_convert \\\n  --root-dir /data \\\n  --dataset all \\\n  --scope snapshot \\\n  --metadata-level both\n</code></pre>"},{"location":"operations/low-memory/","title":"Runbook: Low-Memory Conversion","text":"<p><code>safe</code> is the default profile and is the right answer for low-memory hosts.  No flags needed beyond <code>--root-dir</code> and <code>--dataset</code>:</p> <pre><code>openalex-snapshot convert \\\n  --root-dir /data \\\n  --dataset works\n</code></pre> <p>If you need to constrain memory further (e.g. on an 8 GB box, or to leave headroom for other processes):</p> <pre><code>openalex-snapshot convert \\\n  --root-dir /data \\\n  --dataset works \\\n  --max-memory-mb 4096\n</code></pre> <p>Notes: - <code>safe</code> runs one worker at a time with a generous per-worker memory cap (45 % of   usable RAM, clamped to 8\u201324 GiB on single-worker mode).  This is sufficient to convert   the largest works files (~1 GB compressed \u2192 ~15 GB uncompressed JSON) on any host with   spill-to-disk enabled (which is automatic via the DuckDB <code>temp_directory</code> setting). - <code>--max-memory-mb N</code> overrides the profile's per-worker memory cap. - Use repeated <code>--input-file &lt;rel-path&gt;</code> to isolate and retry specific problematic files. - Spill-to-disk writes intermediate data to   <code>&lt;root&gt;/openalex-snapshot_metadata/duckdb_tmp/</code>.  Ensure that filesystem has free   space comparable to your largest decompressed input (10\u201320 GB is generous for works).</p>"},{"location":"operations/low-memory/#when-safe-is-not-enough","title":"When <code>safe</code> is not enough","text":"<p>If you observe OOM even under <code>safe</code>:</p> <ol> <li>Reduce concurrency: <code>--workers 1</code> is already the safe default; nothing to tighten.</li> <li>Lower memory cap: <code>--max-memory-mb 2048</code> forces a smaller DuckDB budget so it    spills earlier (slower but safer).</li> <li>Pre-split large gz files: <code>--split-size 256mb</code> decomposes large inputs into    chunks before conversion, producing numbered parquets (<code>part_0000_001.parquet</code>,    <code>part_0000_002.parquet</code>, \u2026).</li> </ol>"},{"location":"operations/recovery/","title":"Runbook: Recovery and Resume","text":""},{"location":"operations/recovery/#resume-convert","title":"Resume convert","text":"<p>Re-run the same convert command; existing outputs are skipped.</p>"},{"location":"operations/recovery/#repair-failed-files-built-into-convert","title":"Repair failed files (built into convert)","text":"<p>If <code>verify_convert</code> reported failures, just re-run <code>convert</code>:</p> <pre><code>openalex-snapshot convert --root-dir /data\n</code></pre> <p>Convert reads the latest <code>verify_convert</code> report on startup and deletes any output parquet flagged as bad, so the normal skip-if-exists loop re-builds it.</p> <p>To skip auto-repair (process only files whose parquet is missing):</p> <pre><code>openalex-snapshot convert --root-dir /data --auto-repair=false\n</code></pre> <p>To force a single specific file regardless of the verify report:</p> <pre><code>openalex-snapshot convert --root-dir /data --input-file &lt;rel/path/to/file.gz&gt;\n</code></pre> <p>(<code>--input-file</code> always wins over auto-repair.)</p>"},{"location":"operations/recovery/#diagnose-failures","title":"Diagnose failures","text":"<ul> <li><code>openalex-snapshot report --root-dir /data --latest --full</code></li> <li><code>openalex-snapshot progress --root-dir /data --once</code></li> </ul>"}]}