[workspace]
resolver = "2"
members = [
    "openalex-core",
    "openalex-snapshot",
]

# Shared dependency versions — both crates pick them up via `workspace = true`.
[workspace.dependencies]
anyhow = "1"

[workspace.package]
version = "0.5.0"
edition = "2021"
license = "MIT"
